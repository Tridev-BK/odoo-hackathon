# Models

Runtime models are represented by Prisma in `backend/prisma/schema.prisma`.

This folder is reserved for future domain model mappers when API response shapes diverge from persistence models.
